use_default_settings: true

general:
  debug: false
  instance_name: "Local Private Search"

search:
  safe_search: 0
  autocomplete: "duckduckgo"
  formats:
    - html
    - json

server:
  secret_key: "SECRET_KEY"
  bind_address: "0.0.0.0"